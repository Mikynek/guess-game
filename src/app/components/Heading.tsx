import React from "react";

const Heading: React.FC = () => (
  <div className="flex flex-col items-center">
    <h1 className="text-3xl font-extrabold uppercase bg-clip-text text-transparent bg-gradient-to-r from-gray-600 to-blue-500">
      Guess a Game
    </h1>
  </div>
);

export default Heading;
